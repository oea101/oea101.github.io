import type {Entry} from './site-content';
export default function EntryList({entries,onTag,activeTag}:{entries:Entry[];onTag?:(tag:string)=>void;activeTag?:string}){return <div>{entries.map(entry=><article key={entry.url} className="entry"><h2><a href={entry.url}>{entry.title}</a></h2><p className="description">{entry.description}</p>{(entry.date||entry.detail)&&<p className="meta">{entry.date}{entry.date&&entry.detail?' · ':''}{entry.detail}</p>}{entry.tags&&<div className="entry-tags" aria-label="Article topics">{entry.tags.map(tag=><button type="button" className="topic-pill" key={tag} aria-pressed={activeTag===tag} onClick={()=>onTag?.(tag)}>{tag}</button>)}</div>}</article>)}</div>}
